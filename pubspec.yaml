name: dump_widget_tree
description: Flutter helper function that print Widget/Element/RenderObject/Layer trees, side-by-side.
version: 0.1.2
repository: https://github.com/Ticore/dump_widget_tree
homepage: https://github.com/Ticore/dump_widget_tree

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  test:
  flutter_test:
    sdk: flutter

flutter:

